apiVersion: apps/v1
kind: Deployment
metadata:
  name: order5-service
  namespace: bt-api
  labels:
    app: order5-service
    service: order5
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order5-service
  template:
    metadata:
      labels:
        app: order5-service
        service: order5
    spec:
      containers:
      - name: order5-service
        image: bt_api-order5:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: DEBUG
          valueFrom:
            configMapKeyRef:
              name: bt-api-config
              key: DEBUG
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order5-service
  namespace: bt-api
  labels:
    app: order5-service
spec:
  selector:
    app: order5-service
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
