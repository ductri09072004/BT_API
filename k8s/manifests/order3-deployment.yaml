apiVersion: apps/v1
kind: Deployment
metadata:
  name: order3-service
  namespace: bt-api
  labels:
    app: order3-service
    service: order3
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order3-service
  template:
    metadata:
      labels:
        app: order3-service
        service: order3
    spec:
      containers:
      - name: order3-service
        image: bt_api-order3:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: DEBUG
          valueFrom:
            configMapKeyRef:
              name: bt-api-config
              key: DEBUG
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order3-service
  namespace: bt-api
  labels:
    app: order3-service
spec:
  selector:
    app: order3-service
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
