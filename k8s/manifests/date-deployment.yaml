apiVersion: apps/v1
kind: Deployment
metadata:
  name: date-service
  namespace: bt-api
  labels:
    app: date-service
    service: date
spec:
  replicas: 2
  selector:
    matchLabels:
      app: date-service
  template:
    metadata:
      labels:
        app: date-service
        service: date
    spec:
      containers:
      - name: date-service
        image: bt_api-date:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: DEBUG
          valueFrom:
            configMapKeyRef:
              name: bt-api-config
              key: DEBUG
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: date-service
  namespace: bt-api
  labels:
    app: date-service
spec:
  selector:
    app: date-service
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
